function Invoke-FilePermissionGod {
    <#
    ideally id like to create a template for what shares should look like and make sure that any custom directory that is specified looks like the template including child-objects.

    also reports changes and maybe tests to make sure everything looked proper after applying changes
    #> 
    }