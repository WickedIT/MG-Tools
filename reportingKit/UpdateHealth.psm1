function Invoke-UpdateHealth {
    <#
    Command: (Get-WsusServer).GetSubscription().GetLastSynchronizationInfo()
        Calls the info from the last WSUS sync job -- not the currently running job.

    collect sync info

    collect status of computers with remaining critical patches and health of device
    
    #> 
    }